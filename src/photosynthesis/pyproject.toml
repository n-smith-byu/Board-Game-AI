[project]
name = "photosynthesis"
version = "0.1.0"
description = "Linear programming and AI models for photosynthesis"
authors = [{name = "Nicholas Smith", email = "nicholas.smith0014@gmail.com"}]
requires-python = ">=3.12,<3.15"

dependencies = [
    "torch>=2.9.1",
    "numpy>=2.4.0",
    "tqdm>=4.67.1",
    "torch-geometric>=2.7.0"
]

[tool.poetry.dependencies]
board-game = { path = "../board_game", develop = true }